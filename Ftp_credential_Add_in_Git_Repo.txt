Safest CI/CD Pipeline:
______________________________


1. GitHub Secrets এ Credentials Save করুন
____________________________________________________

A. আপনার GitHub repository তে যান।

B. উপরে Settings → Secrets and variables → Actions এ যান। (Settings er access thakte hobe).

C. New repository secret এ ক্লিক করুন।

নিচের মতো সেভ করুন:
__________________________________________

Name: FTP_SERVER
Value: ftp.softvencefsd.xyz

Name: FTP_USERNAME
Value: laurensw2025@laurensw2025.softvencefsd.xyz

Name: FTP_PASSWORD
Value: fjk84732rhesio




And 
.github -> workflow -> dev.yml 
will be like this :
____________________________

name: 🚀 Deploy via FTP

on:
  push:
    branches:
      - dev

jobs:
  ftp-deploy:
    name: 📦 Upload to Softvencefsd FTP Server
    runs-on: ubuntu-latest

    steps:
    - name: ⏬ Checkout Source Code
      uses: actions/checkout@v3

    - name: 📁 FTP Deploy
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        protocol: ftp
        port: 21
        local-dir: ./  # Project root directory
        server-dir: /  # Change to your target folder
        exclude: |
          **/.git*
          **/.github*
          node_modules/
          vendor/
          .env
